import { Container } from "react-bootstrap";
import styles from './Button.module.css';

const BrowseMenu = () => {
    return (
        <Container>
            <h1>Placeholder</h1>
            <p>Hello There</p>
        </Container>
    );
};

export default BrowseMenu;